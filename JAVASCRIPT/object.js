// Object

const person = {
  name: 'Lele',
  age: 20,
  weight: 73.5,
  isAdmin: false
}

console.log(person.name)
console.log(`${person.name} tem ${person.age} anos e pesa ${person.weight} kilos... Admin:${person.isAdmin}`)

// Array

const animals = [
  
]